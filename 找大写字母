#include <stdio.h>
#include <string.h>
int main()
{
	char arr[101];
	int i,t;
	t = 0;

	gets(arr);
	for (i = 0; i < 100; i++)
	{
		if ((arr[i] >= 'A' && arr[i] <= 'Z'))
		{
			t++;
		}
	}
	printf("%d", t);
	return 0;
}
