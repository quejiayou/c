#include <stdio.h>
#include <string.h>
int main()
{
    char arr[] = "China";
    int i = 0;
    int num = strlen(arr);
    for (i = 0; i < num; i++)
        printf("%c", arr[i] + 4);
    return 0;
}
