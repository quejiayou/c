#include <stdio.h>
int gcd(int a, int b)
{
	int c = 0;
	while (b > 0)
	{
		c = a % b;
		a = b;
		b = c;
	}
	return a;
}
int main()
{
	int n, m;
	int i;
	m = 0;
	printf("输入:");
	scanf_s("%d", &n);
	for (i = 2; i <= n; i++)
		if (gcd(i, n) == 1)
			m++;
	printf("%d", m);
	return 0;
}
