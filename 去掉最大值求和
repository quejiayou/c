#include <stdio.h>
#include <string.h>
int main()
{
	int a[5];
	int max, b=0;
	int i ;
	for(i = 0; i < 5; i++)
	{
		scanf("%d", &a[i]);
	} 
	max = a[0];
	for(i = 0; i < 5; i++)
	{
		b+=a[i];
		if(a[i] > max)
		{
			max=a[i];
		}
	}
	
	printf("%d",b-max);
		return 0;
} 
